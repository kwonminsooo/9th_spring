객체 그래프 탐색은 객체들이 서로 참조로 연결된 구조를 따라가면서 필요한 데이터를 꺼내는 것을 의미한다. 즉, 하나의 객체 안에 또 다른 객체가 포함되어 있을 때, 그 참조를 계속 이어가며 값을 조회하는 과정 전체를 말한다. JPA 같은 ORM에서는 이 탐색 과정이 자동으로 이루어지기 때문에 개발자는 객체만 접근하지만, 내부적으로는 연관된 엔티티를 불러오기 위해 추가 쿼리가 발생하거나 지연 로딩 때문에 예외가 생길 수 있어, 객체 간 연관 관계를 어떻게 구성하고 탐색하는지가 성능과 안정성에 중요한 영향을 준다.